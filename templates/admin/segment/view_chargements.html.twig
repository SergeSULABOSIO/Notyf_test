<!DOCTYPE html>
<html>
    <head>
        <title>My Webpage</title>
    </head>
    <body>
        <table class="table table-striped">
            <thead>
                <tr>
                <th scope="col">N°</th>
                <th scope="col">Type</th>
                <th scope="col">Description</th>
                <th scope="col">Montant</th>
                </tr>
            </thead>
            <tbody>
                {% set i = 1 %}
                {% for chargement in field.value %}
                    <tr>
                        <td scope="row">{{ i }}</td>
                        <th>{{chargement.typeText}}</th>
                        <td>{{chargement.description}}</td>
                        <td>{{(chargement.montant / 100)|format_currency(chargement.monnaie_Affichage.code)}}</td>
                    </tr>
                    {% set i = i + 1 %}
                {% endfor %}
            </tbody>
        </table>
    </body>
</html>