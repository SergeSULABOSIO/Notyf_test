{# Entête de la page #}
<div class="header">
    {# barre du logo #}
    <table class="tableau-bordure-zero centre" style="width: 100;">
        <tr>
            <td>
                <img src="{{imageSrc}}" class="image-centre"> 
            </td>
        </tr>
        <tr>
            <td class="texte-centre">
                <span class="texte-petit">{{facture.entreprise.nom}}</span><br>
            </td>
        </tr>
    </table>
</div>